#!/usr/bin/env bash

# This does nothing for now

# No need to write the license to each node, it is stored in the backend storage

# Debugging purposes
echo "$(date): This is a follower node. It will join the active node." |sudo tee -a /home/ubuntu/_vaultSetup/vault_debug
